package frc.robot.subsystems.climber.config;

import frc.robot.shared.config.AbstractMotorConfig;

/**
 * Configuration bundle for the climber motor controller and its travel limits.
 * <p>
 * Values are stored in degrees for readability and exposed through tunable suppliers so the climber can be adjusted without redeploying. This class
 * is a placeholder until motor hardware is selected.
 * </p>
 */
public class ClimberMotorConfig extends AbstractMotorConfig {
    // Intentionally empty; inherits tunable motor fields and suppliers.
}
