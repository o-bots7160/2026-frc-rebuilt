package frc.robot.subsystems.climber.config;

import frc.robot.shared.config.AbstractMotorConfig;

/**
 * Configuration bundle for the climber motor controller and its travel limits.
 * <p>
 * Values are deserialized from JSON and exposed through plain getters inherited from {@link AbstractMotorConfig}. This class is a placeholder until
 * motor hardware is selected. Changes require a redeploy.
 * </p>
 */
public class ClimberMotorConfig extends AbstractMotorConfig {
    // Intentionally empty; inherits motor fields and getters.
}
